# PLMS Invariants Checker Configuration
# Source this file before running wrapper script

# Database path (adapt for production)
DB_PATH="${DB_PATH:-artifacts/registry/registry.db}"

# Slack webhook URL for alerts (optional)
# Get this from Slack: Apps → Incoming Webhooks → Add New Webhook
SLACK_WEBHOOK_URL="${SLACK_WEBHOOK_URL:-}"

# Email for critical alerts (optional)
# Requires sendmail or equivalent MTA configured
ALERT_EMAIL="${ALERT_EMAIL:-}"

# PagerDuty routing key (optional, for critical failures)
PD_ROUTING_KEY="${PD_ROUTING_KEY:-}"

# Environment name (for alert messages)
ENV_NAME="${ENV_NAME:-dev}"

# Redis URL (for idempotency cache health check)
REDIS_URL="${REDIS_URL:-redis://localhost:6379/0}"

# Project root directory
# Autodetect from script location, or override
PROJECT_ROOT="${PROJECT_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
